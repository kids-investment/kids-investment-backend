service: api-backend
runtime: nodejs16
instance_class: F2

env_variables:
  DB_USER: $USER
  DB_BACKEND: $PASSWORD

handlers:
- url: /.*
  secure: always
  redirect_http_response_code: 301
  script: auto
